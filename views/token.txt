
app token <command>

Usage:

app token --count                             displays a count of the tokens created
app token --new <user>                        generates a token for a given username, saves tokens to the json file
app token --set <user> <attribute> <value>    assign an attribute for a user with a given value
app token --findUser <user>                   fetches a token for a given username

